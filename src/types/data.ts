

export interface DataProps {
    img: string
    title: string
    price: string
    id: number
    brand:string
    slug:string
}
export interface bannerType {
    img: string
    title: string
    id: number
}